(module
    ;; TODO: uncomment when we support funcref in whamm
    ;;  (func $insert_probe (export "insert_new_probe") (param i32 i32 funcref) (result i32))
  (func $remove_probe (export "remove_probe") (param i32))
)